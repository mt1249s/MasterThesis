import torch
import torchvision
from torch.utils.data import Dataset, DataLoader
import numpy as np
import math

class WinDataset(Dataset):
    def __init__(self):
        #loading dataset

    def __getitem__(self, indx):
        #indexing dataset[0]







